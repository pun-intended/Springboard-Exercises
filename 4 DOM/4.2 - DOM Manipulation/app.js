        // //Select the section with an id of container without using querySelector.
        //const contGetElement = document.getElementById("container");
        //console.log(contGetElement);
        // //Select the section with an id of container using querySelector.
        // const contQuery = document.querySelector("container");
        // //Select all of the list items with a class of “second”.
        // const allLis = document.getElementsByClassName("second");
        // //Select a list item with a class of third, but only the list item inside of the ol tag.
        // const thirdLi = document.querySelector("ol").getElementsByClassName("third");
        // //Give the section with an id of container the text “Hello!”.
        // contQuery.append("Hello");
        // //Add the class main to the div with a class of footer.
        // const footer = document.querySelector(".footer")
        // footer.classList().add("main");
        // //Remove the class main on the div with a class of footer.
        // footer.classList().remove("class", "main")
        // //Create a new li element.
        // const newLi = document.createElement("li");
        // //Give the li the text “four”.
        // li.innerText = "four";
        // //Append the li to the ul element.
        // const ulist = document.querySelector("ul")
        // //Loop over all of the lis inside the ol tag and give them a background color of “green”.
        // const allOlLis = document.querySelector("ol").getElementsByTagName("li");
        // for (li of allOlLis){
        //     li.style.backgroundColor = "green"
        // }
        // //Remove the div with a class of footer
        // document.querySelector(".footer").remove();