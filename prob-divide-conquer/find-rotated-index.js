function findRotatedIndex() {
 // start on outside
 // find middle
 // if compare to target and l/r idx
 // if middle > target:
 //     - if target > leftIdx -> move rightIdx to middle -1
 //     - if target < rightIdx -> move leftIdx middle +1
 // if middle < target
 //     - if target > leftIdx
 //     - if target < rightIdx
}

module.exports = findRotatedIndex