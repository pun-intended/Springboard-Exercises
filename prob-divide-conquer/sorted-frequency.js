function sortedFrequency() {
// find rightLimit, then leftLimit
// rightLimit 
// --> find middle, if middle < target lIndex = middle (repeat)
// --> if middle > target 
// - if middle > target, rightIdx = middle (repeat)
// - if middle < target, leftIdx = middle (repeat)
// - if middle == target ->check sides


}

module.exports = sortedFrequency